// import java.uitil.Scanner;

// class primeOrNot {
// public static void main(String[] args) {
// Scanner sc = new Scanner(System.in);
// int num = sc.nextint();

// System.out.print((num == 2 || num == 3) ? (num + "is prime")
// : ((num % 2 == 0 || num % 3 == 0) ? (num + "is prime") : (num + "is
// prime")));
// }
// }